var foo = 3;

var bar: float = 4.42;

func myfunc(a: int, b: float): float {
    var c = as_float(a) * b;
    return c;
}

var baz = myfunc(3) * (as_float(foo) + bar);

if (foo >= 32) {
    print(foo);
} else if (baz >= 12) {
    print(bar);
} else {
    print(baz);
}
